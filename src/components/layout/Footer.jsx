import React from 'react'

export default function Footer() {
  return (
    <footer className="app-footer">
      Built with React + Vite + Leaflet (OpenStreetMap). No API key required.
    </footer>
  )
}
